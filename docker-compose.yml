nodeapp:
    build: app
    container_name: 112_queue_nodeapp
    restart: always
    labels:
        - "112_queue"
    ports:
        - "127.0.0.1:3000:3000"
    environment:
        - NODE_ENV=production
