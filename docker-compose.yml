postgresql:
    image: postgres
    container_name: 112_queue_postgres
    labels: 
        - "112_queue"
    expose:
        - "5432"
    environment:
        - POSTGRES_PASSWORD=supersecret
        - POSTGRES_DB=queue
        - POSTGRES_USER=queue

nodeapp:
    build: app 
    container_name: 112_queue_nodeapp
    links:
        - postgresql
    labels:
        - "112_queue"
    expose:
        - "3000"



